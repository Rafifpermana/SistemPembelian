<!DOCTYPE html>
<html lang="id">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Admin Toko</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
      @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap');
      body { font-family: 'Inter', sans-serif; }
    </style>
  </head>
  <body class="bg-slate-50 min-h-screen text-slate-800">
    
    <nav class="bg-white border-b border-slate-200 sticky top-0 z-50">
      <div class="container mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-16">
          <div class="flex items-center gap-3">
            <div class="bg-blue-600 text-white p-2 rounded-lg shadow-sm">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 11V7a4 4 0 00-8 0v4M5 9h14l1 12H4L5 9z" />
              </svg>
            </div>
            <div>
              <h1 class="text-xl font-bold tracking-tight text-slate-900">Admin Toko</h1>
              <p class="text-xs text-slate-500 font-medium">Dashboard Management</p>
            </div>
          </div>
          <div class="flex items-center">
            <span class="inline-flex items-center px-3 py-1 rounded-full text-xs font-medium bg-green-100 text-green-800">
              <span class="w-2 h-2 bg-green-500 rounded-full mr-2"></span>
              Server Online
            </span>
          </div>
        </div>
      </div>
    </nav>

    <main class="container mx-auto px-4 py-8">
      <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
        
        <div class="lg:col-span-1 space-y-8">
          
          <div class="bg-white rounded-2xl shadow-sm border border-slate-100 overflow-hidden">
            <div class="p-6 bg-white border-b border-slate-100">
              <h2 class="text-lg font-bold text-slate-800 flex items-center gap-2">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-blue-500" viewBox="0 0 20 20" fill="currentColor">
                  <path d="M4 4a2 2 0 00-2 2v1h16V6a2 2 0 00-2-2H4z" />
                  <path fill-rule="evenodd" d="M18 9H2v5a2 2 0 002 2h12a2 2 0 002-2V9zM4 13a1 1 0 011-1h1a1 1 0 110 2H5a1 1 0 01-1-1zm5-1a1 1 0 100 2h1a1 1 0 100-2H9z" clip-rule="evenodd" />
                </svg>
                Transaksi Baru
              </h2>
            </div>
            <div class="p-6">
              <form action="/buy" method="POST" class="space-y-5">
                <div>
                  <label class="block text-sm font-medium text-slate-700 mb-2">Pilih Produk</label>
                  <div class="relative">
                    <select name="product_id" class="w-full pl-4 pr-10 py-3 bg-slate-50 border border-slate-200 rounded-xl focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition appearance-none cursor-pointer" required>
                        <option value="" disabled selected>-- Pilih Barang --</option>
                        <% products.forEach(p => { %>
                            <option value="<%= p.id %>" <%= p.quantity === 0 ? 'disabled' : '' %>>
                                <%= p.name %> (Stok: <%= p.quantity %>) - Rp <%= Number(p.price).toLocaleString('id-ID') %>
                            </option>
                        <% }) %>
                    </select>
                    <div class="absolute inset-y-0 right-0 flex items-center px-3 pointer-events-none text-slate-500">
                      <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path></svg>
                    </div>
                  </div>
                </div>

                <div>
                  <label class="block text-sm font-medium text-slate-700 mb-2">Jumlah</label>
                  <input type="number" name="quantity" class="w-full px-4 py-3 bg-slate-50 border border-slate-200 rounded-xl focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition" min="1" placeholder="0" required>
                </div>

                <button type="submit" class="w-full py-3.5 px-4 bg-gradient-to-r from-blue-600 to-blue-700 hover:from-blue-700 hover:to-blue-800 text-white font-semibold rounded-xl shadow-lg shadow-blue-500/30 transition transform active:scale-95 flex justify-center items-center gap-2">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6" />
                    </svg>
                    Simpan Transaksi
                </button>
              </form>
            </div>
          </div>

          <div class="bg-white rounded-2xl shadow-sm border border-slate-100 overflow-hidden">
            <div class="p-6 border-b border-slate-100 flex justify-between items-center">
              <h2 class="text-lg font-bold text-slate-800 flex items-center gap-2">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-purple-500" viewBox="0 0 20 20" fill="currentColor">
                    <path d="M7 3a1 1 0 000 2h6a1 1 0 100-2H7zM4 7a1 1 0 011-1h10a1 1 0 110 2H5a1 1 0 01-1-1zM2 11a2 2 0 012-2h12a2 2 0 012 2v4a2 2 0 01-2 2H4a2 2 0 01-2-2v-4z" />
                </svg>
                Inventori
              </h2>
            </div>
            <div class="divide-y divide-slate-100 max-h-[500px] overflow-y-auto">
                <% products.forEach(p => { %>
                  <div class="p-4 hover:bg-slate-50 transition flex items-center justify-between group">
                    <div>
                      <h3 class="font-semibold text-slate-800"><%= p.name %></h3>
                      <p class="text-sm text-slate-500">Rp <%= Number(p.price).toLocaleString('id-ID') %></p>
                    </div>
                    <div class="text-right">
                        <span class="px-3 py-1 rounded-lg text-sm font-bold <%= p.quantity < 5 ? 'bg-red-100 text-red-600' : 'bg-green-100 text-green-600' %>">
                            <%= p.quantity %>
                        </span>
                        <% if(p.quantity < 5) { %>
                            <p class="text-[10px] text-red-500 font-medium mt-1 animate-pulse">Stok Menipis!</p>
                        <% } %>
                    </div>
                  </div>
                <% }) %>
            </div>
          </div>

        </div>

        <div class="lg:col-span-2">
          <div class="bg-white rounded-2xl shadow-sm border border-slate-100 overflow-hidden">
            <div class="p-6 border-b border-slate-100 bg-white">
                <h2 class="text-lg font-bold text-slate-800 flex items-center gap-2">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-slate-600" viewBox="0 0 20 20" fill="currentColor">
                        <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm1-12a1 1 0 10-2 0v4a1 1 0 00.293.707l2.828 2.829a1 1 0 101.415-1.415L11 9.586V6z" clip-rule="evenodd" />
                    </svg>
                    Riwayat Transaksi
                </h2>
            </div>
            
            <div class="overflow-x-auto">
              <table class="w-full text-left border-collapse">
                <thead>
                  <tr class="bg-slate-50 text-slate-500 text-xs uppercase tracking-wider">
                    <th class="px-6 py-4 font-semibold">Waktu</th>
                    <th class="px-6 py-4 font-semibold">Produk</th>
                    <th class="px-6 py-4 font-semibold text-center">Jumlah</th>
                    <th class="px-6 py-4 font-semibold text-right">Total</th>
                    <th class="px-6 py-4 font-semibold text-center">Status</th>
                    <th class="px-6 py-4 font-semibold text-center">Pembatalan</th>
                  </tr>
                </thead>
                <tbody class="divide-y divide-slate-100">
                  <% transactions.forEach(t => { %>
                    <tr class="hover:bg-slate-50 transition <%= t.status === 'cancelled' ? 'bg-red-50/50' : '' %>">
                        
                        <td class="px-6 py-4 whitespace-nowrap">
                            <div class="text-sm font-medium text-slate-900">
                                <%= new Date(t.created_at).toLocaleDateString('id-ID', {day: 'numeric', month: 'short'}) %>
                            </div>
                            <div class="text-xs text-slate-400">
                                <%= new Date(t.created_at).toLocaleTimeString('id-ID', {hour: '2-digit', minute: '2-digit'}) %>
                            </div>
                        </td>

                        <td class="px-6 py-4 text-sm text-slate-700 font-medium">
                            <%= t.name %>
                        </td>

                        <td class="px-6 py-4 text-center">
                            <span class="inline-flex items-center justify-center w-8 h-8 bg-slate-100 rounded-full text-xs font-bold text-slate-600">
                                <%= t.quantity %>
                            </span>
                        </td>

                        <td class="px-6 py-4 text-right text-sm font-bold text-slate-800">
                            Rp <%= Number(t.total_price).toLocaleString('id-ID') %>
                        </td>

                        <td class="px-6 py-4 text-center">
                            <% if(t.status === 'completed') { %>
                                <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-green-100 text-green-800 border border-green-200">
                                    Sukses
                                </span>
                            <% } else { %>
                                <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-red-100 text-red-800 border border-red-200">
                                    Dibatalkan
                                </span>
                            <% } %>
                        </td>

                        <td class="px-6 py-4 text-center">
                            <% if(t.status === 'completed') { %>
                                <form action="/cancel/<%= t.id %>" method="POST" onsubmit="return confirm('Batalkan transaksi ini? Stok akan dikembalikan.')">
                                    <button type="submit" class="text-red-500 hover:text-red-700 hover:bg-red-50 p-2 rounded-lg transition" title="Batalkan">
                                        <form action="/cancel/<%= t.id %>" method="POST" onsubmit="return confirm('Batalkan transaksi ini? Stok akan dikembalikan.')">
                                            <button type="submit" class="text-xs font-bold text-red-600 bg-red-50 hover:bg-red-100 hover:text-red-800 px-3 py-1.5 rounded-lg border border-red-100 transition duration-200">
                                                Batalkan
                                            </button>
                                        </form>
                                    </button>
                                </form>
                            <% } else { %>
                                <span class="text-slate-300">-</span>
                            <% } %>
                        </td>
                    </tr>
                  <% }) %>
                </tbody>
              </table>
              
              <% if(transactions.length === 0) { %>
                <div class="p-8 text-center text-slate-400 bg-slate-50 m-4 rounded-xl border border-dashed border-slate-200">
                    <p>Belum ada transaksi hari ini.</p>
                </div>
              <% } %>
            </div>
          </div>
        </div>

      </div>
    </main>
  </body>
</html>